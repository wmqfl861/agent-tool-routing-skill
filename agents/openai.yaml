interface:
  display_name: "Tool Routing Architecture"
  short_description: "Manage tool indexing, intent routes, and safe offboarding."
  default_prompt: "Use $tool-routing-architecture to build validated A/B routes, retain C exclusions, and complete managed offboarding from concise tool-removal requests."
